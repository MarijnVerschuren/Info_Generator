from info_gen import *
import json


if __name__ == "__main__":
	"""
	g = info_gen()
	p = next(g)

	data = json.dumps(p, cls=info_encoder)
	json.loads(data, cls=info_decoder)
	"""

	# TODO: phone, iban, photo (this person does not exist)